/**
 * Service layer placeholders for future API integration.
 * No network calls — frontend foundation only.
 */

export const services = {
  agents: null,
  users: null,
  logs: null,
  notifications: null,
} as const
